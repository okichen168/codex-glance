# Privacy

Codex Glance communicates only with the local official `codex app-server` to read usage information. It reads rate-limit and usage data needed to display the floating window.

It does not read cookies, access tokens, chats, source code, or files, and it does not upload data to an OkiChen-operated server. GitHub may process download and release-page access under GitHub's own privacy policy.
